<?xml version="1.0" encoding="UTF-8"?>

<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"

         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

         xsi:schemaLocation="
         https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"

         version="6.0">

    <servlet>

        <servlet-name>
            dispatcher
        </servlet-name>

        <servlet-class>
            org.springframework.web.servlet.DispatcherServlet
        </servlet-class>

        <init-param>

            <param-name>
                contextConfigLocation
            </param-name>

            <param-value>
                /WEB-INF/dispatcher-servlet.xml
            </param-value>

        </init-param>

        <load-on-startup>
            1
        </load-on-startup>

    </servlet>

    <servlet-mapping>

        <servlet-name>
            dispatcher
        </servlet-name>

        <url-pattern>
            /
        </url-pattern>

    </servlet-mapping>

    <filter>
        <filter-name>authFilter</filter-name>
        <filter-class>uef.edu.vn.filter.AuthFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>authFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
        <url-pattern>/librarian/*</url-pattern>
        <url-pattern>/reader/*</url-pattern>
        <url-pattern>/borrow/*</url-pattern>
    </filter-mapping>

    <filter>
        <filter-name>roleFilter</filter-name>
        <filter-class>uef.edu.vn.filter.AdminFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>roleFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
        <url-pattern>/librarian/*</url-pattern>
        <url-pattern>/reader/*</url-pattern>
        <url-pattern>/borrow/*</url-pattern>
    </filter-mapping>

</web-app>
